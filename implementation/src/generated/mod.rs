// Generated module. `config.rs` is (re)produced by
// `cargo anypoint config-gen` from `definition/gcl.yaml` during
// `make build`. The checked-in copy mirrors that output so the
// host-side `cargo test` suite compiles without the codegen step.
pub mod config;
